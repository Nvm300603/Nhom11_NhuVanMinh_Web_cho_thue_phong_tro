<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partical/head'); %>
</head>

<body class="bg-gray-100 font-sans leading-normal tracking-normal">

    <!-- Header -->
    <%- include('../partical/navbar'); %>

    <!-- Main Content -->
    <main class="container mx-auto my-8 px-4">
        <!-- Filter Section -->
        <!-- <section class="mb-8">
            <h2 class="text-2xl font-bold mb-4">Bộ Lọc Tìm Phòng</h2>
            <form id="filter-form" class="flex flex-wrap gap-4">
                <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/4">
                    <label for="location" class="block text-sm font-medium text-gray-700">Khu Vực</label>
                    <select id="location" name="location" class="mt-1 block w-full p-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500">
                        <option value="">Chọn Khu Vực</option>
                        <option value="area1">Khu Vực 1</option>
                        <option value="area2">Khu Vực 2</option>
                    </select>
                </div>
                <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/4">
                    <label for="price" class="block text-sm font-medium text-gray-700">Giá Cả (VND)</label>
                    <input type="number" id="price" name="price" class="mt-1 block w-full p-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500" placeholder="Ví dụ: 1000000">
                </div>
                <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/4">
                    <label for="area" class="block text-sm font-medium text-gray-700">Diện Tích (m2)</label>
                    <input type="number" id="area" name="area" class="mt-1 block w-full p-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500" placeholder="Ví dụ: 20">
                </div>
                <div class="w-full sm:w-1/2 md:w-1/3 lg:w-1/4">
                    <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 focus:outline-none focus:bg-blue-600">Tìm Kiếm</button>
                </div>
            </form>
        </section> -->

        <!-- Room List Section -->
        <section class="mb-8">
            <h2 class="text-2xl font-bold mb-4">Danh Sách Phòng Trọ</h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                <% rooms.forEach(room => { %>
                    <div class="bg-white shadow-md p-4 rounded-md">
                        <a href="/rooms/<%= room.slug %>" class="block mb-4">
                            <img src="<%= room.image %>" alt="<%= room.title %>" class="w-full h-auto object-cover" style="width: 200px; height: 200px;">
                        </a>
                        <div>
                            <h3 class="text-lg font-semibold mb-2"><%= room.title %></h3>
                            <p class="text-gray-600 mb-2">Khu Vực: <%= room.address %></p>
                            <p class="text-gray-600 mb-2">Giá: <%= Number(room.price).toLocaleString('vi-VN') %> VND</p>
                            <p class="text-gray-600 mb-2">Diện Tích: <%= Number(room.area).toLocaleString('vi-VN') %>m<sup>2</sup></p>
                        </div>
                    </div>
                <% }); %>
            </div>
        </section>
        
        <!-- Pagination -->
        <nav class="flex justify-center mt-8">
            <ul class="flex space-x-4">
                <% if (pagination.totalPages > 1) { %>
                    <% if (pagination.page > 1) { %>
                        <li><a href="#" onclick="redirectToNewPage(<%= pagination.page - 1 %>)" class="px-4 py-2 bg-gray-200 text-gray-700 rounded-md hover:bg-gray-300">Previous</a></li>
                    <% } else { %>
                        <li><span class="px-4 py-2 bg-gray-200 text-gray-400 rounded-md cursor-not-allowed">Previous</span></li>
                    <% } %>
                    
                    <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                        <li><a href="#" onclick="redirectToNewPage(<%= i %>)" class="px-4 py-2 bg-white text-gray-700 rounded-md hover:bg-gray-200 <%= i === pagination.page ? 'font-bold' : '' %>"><%= i %></a></li>
                    <% } %>
                    
                    <% if (pagination.page < pagination.totalPages) { %>
                        <li><a href="#" onclick="redirectToNewPage(<%= pagination.page + 1 %>)" class="px-4 py-2 bg-gray-200 text-gray-700 rounded-md hover:bg-gray-300">Next</a></li>
                    <% } else { %>
                        <li><span class="px-4 py-2 bg-gray-200 text-gray-400 rounded-md cursor-not-allowed">Next</span></li>
                    <% } %>
                <% } else { %>
                    <li><span class="px-4 py-2 bg-gray-200 text-gray-400 rounded-md cursor-not-allowed">Previous</span></li>
                    <li><a href="#" onclick="redirectToNewPage(1)" class="px-4 py-2 bg-white text-gray-700 rounded-md hover:bg-gray-200 font-bold">1</a></li>
                    <li><span class="px-4 py-2 bg-gray-200 text-gray-400 rounded-md cursor-not-allowed">Next</span></li>
                <% } %>
            </ul>
        </nav>

        
    </main>

    <!-- Footer -->
    <%- include('../partical/foot'); %>

</body>

</html>
